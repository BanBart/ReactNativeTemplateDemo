import { DefaultTheme } from '@react-navigation/native'

const reactNavigationTheme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    background: 'white',
  },
}

export { reactNavigationTheme }
